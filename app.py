import streamlit as st
import google.generativeai as genai

st.set_page_config(page_title="NCTB AI Tutor", layout="wide")

# মাস্টার ইনস্ট্রাকশন
MASTER_PROMPT = """
তুমি একজন বিশেষজ্ঞ এআই টিউটর। আপলোড করা PDF থেকে ২০২৬ সালের শিক্ষাক্রম অনুযায়ী কাজ করো।
১. সারমর্ম: ৫ লাইন (সাধারণ ফন্ট, কঠিন শব্দের পাশে বন্ধনীতে সহজ অর্থ)।
২. ১০টি সৃজনশীল কাজ: (সিরিয়ালসহ বাধ্যতামূলক)।
৩. ১০টি শব্দার্থ ও ১০টি এমসিকিউ: (সিরিয়ালসহ)।
৪. শো-কন্ট্রোল: শুরুতে ১০টি দেখাবে, বাকিগুলোর জন্য [➕ আরো দেখুন] এবং [➖ কম দেখুন] রাখবে।
৫. উত্তর: শেষে "--- উত্তর নিচে দেওয়া হলো ---" সেকশনে থাকবে।
"""

st.title("📚 ২০২৬ স্মার্ট এআই টিউটর")

with st.sidebar:
    api_key = st.text_input("Gemini API Key দিন:", type="password")
    chapter = st.selectbox("অধ্যায় নির্বাচন করুন:", ["অধ্যায় ১", "অধ্যায় ২", "অধ্যায় ৩"])

if api_key:
    genai.configure(api_key=api_key)
    model = genai.GenerativeModel("gemini-1.5-flash", system_instruction=MASTER_PROMPT)
    uploaded_file = st.file_uploader("বইয়ের PDF আপলোড করুন", type=["pdf"])

    if uploaded_file:
        if st.button(f"🚀 {chapter} লোড করো"):
            with st.spinner("তৈরি হচ্ছে..."):
                q = f"তুমি {chapter} এর সারমর্ম, ১০টি সৃজনশীল কাজ, ১০টি শব্দার্থ এবং ১০টি এমসিকিউ একবারে দাও।"
                res = model.generate_content([uploaded_file, q])
                st.session_state.result = res.text

        if 'result' in st.session_state:
            st.markdown(st.session_state.result)
            st.divider()
            # কুইক বাটন
            c1, c2, c3 = st.columns(3)
            with c1: 
                if st.button("🔘 আরো শব্দার্থ"): st.write(model.generate_content([uploaded_file, "আরো ১০টি শব্দার্থ দাও"]).text)
            with c2:
                if st.button("🔘 আরো এমসিকিউ"): st.write(model.generate_content([uploaded_file, "আরো ১০টি এমসিকিউ দাও"]).text)
            with c3:
                if st.button("🔘 ১০টি সৃজনশীল"): st.write(model.generate_content([uploaded_file, "আরো ১০টি সৃজনশীল দাও"]).text)
else:
    st.warning("চালু করতে সাইডবারে API Key দিন।")
